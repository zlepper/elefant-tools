[package]
name = "benchmark-import-prepare"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { workspace = true, features = ["rt", "rt-multi-thread", "macros", "fs"] }
elefant-tools = { workspace = true, features = ["test_utilities"] }
anyhow = { workspace = true }
